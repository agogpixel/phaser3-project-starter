name: Build & Test
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  Build-And-Test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build
        run: echo todo
      - name: Test
        run: echo todo
